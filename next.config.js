/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'store.storeimages.cdn-apple.com',
      "images.unsplash.com"
    ],
  },
}

module.exports = nextConfig; 